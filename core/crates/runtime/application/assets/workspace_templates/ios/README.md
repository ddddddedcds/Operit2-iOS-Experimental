# iOS Project

iOS application project workspace.

This workspace is for **editing** project sources on the device. Building and
signing an iOS app requires macOS + Xcode, so there are no run commands here —
open the project on your Mac to build it.

Scaffold a real structure (for example with XcodeGen `project.yml`) and keep the
sources here if you want them managed inside Operit2.
