[package]
name = "operit-workflow-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
async-trait = "0.1"
operit-host-api = { path = "../operit-host-api" }
operit-model = { version = "2.0.0-preview.5", path = "../operit-model" }
regex = "1"
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["rt", "sync", "time", "macros"] }

[dev-dependencies]
uuid.workspace = true
